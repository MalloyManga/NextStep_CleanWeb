# CleanWeb 开发环境变量

# 开发时复制本文件并改名为 `.env` 文件，然后填写自己的 API Key。WXT 会通过 `wxt.config.ts` 注入到 `utils/llm.ts` 中。

VITE_LLM_API_KEY=sk-xxxxxxxx
VITE_LLM_BASE_URL=https://api.openai.com/v1
VITE_LLM_MODEL=gpt-4o-mini

# 生产环境（用户安装后的插件）不读取这些变量。用户需要在插件的 Settings 页填写自己的 API Key。

# `.env` 文件已加入 `.gitignore`，不会被提交。
